<% if @video_course.init? %>
  <%= link_to(t('view.course_info_show.start_learning2'), "javascript:alert('#{t('view.live_studio/courses.show.learn_alert')}');", class: 'btn-startclass action') %>
<% else %>
  <%= link_to(t('view.course_info_show.start_learning2'), live_studio.play_video_lesson_path(@video_course.video_lessons.first), target: '_blank', class: 'btn-view action') if @video_course.published? %>
<% end %>
