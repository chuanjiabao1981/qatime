<ul class="item-handpick" id="video_courses">
  <% courses.each do |course| %>
    <li class="col-md-4 col-sm-4 col-xs-6">
      <%= link_to live_studio.video_course_path(course), target: '_blank' do %>
        <%= image_tag course.publicize_url(:info), class: 'handle_error_list' %>
      <% end %>
      <div class="handpick-info">
        <p>
          <%= link_to live_studio.video_course_path(course), target: '_blank' do %>
            <strong><%= truncate_u(course.name, length: 12) %></strong>
          <% end %>
        </p>
        <p>
          <span class="class-price"><%= number_to_currency(course.current_price, locale: :cn) %></span>
          <span class="class-number"><%= t('view.course_show.buy_tickets_count_text', count: course.buy_user_count) %></span>
        </p>
      </div>
    </li>
  <% end %>
</ul>