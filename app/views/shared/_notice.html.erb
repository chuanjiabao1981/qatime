<% if flash.present? %>
  <!-- 有提示消息显示提示消息 -->
  <%= render 'shared/flash_msg' %>
<% elsif current_user && current_user.student_or_teacher? && !current_user.try(:cash_account_password?)  %>
  <!-- 没有提示消息 -->
  <%= render 'shared/pay_password_warning' %>
<% end %>

