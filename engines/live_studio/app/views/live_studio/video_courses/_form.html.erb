<%= form_for(@video_course) do |f| %>
  <% if @video_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_course.errors.count, "error") %> prohibited this video_course from being saved:</h2>

      <ul>
      <% @video_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
