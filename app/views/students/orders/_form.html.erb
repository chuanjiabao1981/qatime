<%= simple_form_for(@students_order) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :order_no %>
    <%= f.association :product %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
