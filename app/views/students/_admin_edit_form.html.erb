<div class="row" style="margin-bottom: 30px;">
  <h2 class="text-center"><%= t(".edit_student") %></h2>
</div>
<% %w(password login_mobile email parent_phone).each do |attr| %>
<div class="row" >
<%= simple_form_for(@student, url: admin_update_student_path(@student), remote: :ture) do |f| %>
  <div class="col-md-2 col-md-offset-3 text-right form_lable">
    <%= t ".#{attr}_label" %>
  </div>
  <div class="col-md-3">
    <%= f.input attr.to_sym, label: false %>
  </div>
  <div class="col-md-3">
    <%= f.submit t(".submit"), id: "submit_#{attr}_#{@student.id}", class: "btn btn-default"%>
  </div>
  <% end %>
</div>
<% end %>
