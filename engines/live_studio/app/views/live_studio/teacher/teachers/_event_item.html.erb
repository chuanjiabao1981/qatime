<li class-date="<%= item.class_date.to_s %>">
  <div class="row">
    <div class="col-md-2 schedule-pic">
      <%= link_to live_studio.customized_group_path(item.group_id), class: 'education-label' do %>
        <%= image_tag item.group.publicize_url(:small), class: 'handle_error_list' %>
        <p class="status-exclusive"><%= t('view.student.students.schedules.lesson.customized_group') %></p>
      <% end %>
    </div>
    <div class="col-md-6 schedule-msg">
      <div class="row">
        <div class="col-md-12 education-title">
          <p>
            <%= link_to item.name, live_studio.customized_group_path(item.group_id) %>
          </p>
        </div>
        <div class="col-md-12 col-sm-6 education-line">
          <span><%= item.group.grade %></span>
        </div>
        <div class="col-md-12 col-sm-6 education-line">
          <span><%= t('view.student.students.schedules.lesson.student_count') %><em><%= item.group.buy_user_count %></em><%= t('view.common.people') %></span>
        </div>
      </div>
    </div>
    <div class="col-md-4 schedule-time">
      <div class="row">
        <div class="col-md-12 col-sm-6 education-line">
          <span><%= item.status_text %></span>
        </div>
        <div class="col-md-12 col-sm-6 education-line">
          <span><%= item.class_date %> <%= item.live_time %></span>
        </div>
      </div>
    </div>
  </div>
</li>
