<%= simple_form_for([:admin, @position]) do |f| %>

  <%= f.input :name %>
  <%= f.input :klass_name, collection: Recommend::Item.subclasses.map{|c| [c.model_name.human, c.model_name.name] }  %>

  <%= f.submit %>
<% end %>
