$(".question-list").prepend("<%= j render(@question)  %>");
$("#question-form").hide();
$('#question a.cancel').hide().siblings().show(); // 按钮切换
$(".question-list").parent().find('.info-notfound').hide();
