<div class="qa_box">
  <div class="qa_big_cell">
    <div>
      <h2><%=RegisterCode.model_name.human%>列表</h2>
        <% if current_user.manager? %>
            <span><%= link_to "增加#{RegisterCode.model_name.human}",new_managers_register_code_path%></span>
        <% end %>
    </div>

  </div>
  <div>

    <%= render partial: "admins/register_codes/register_code" , collection: @register_codes %>
    <%= will_paginate @register_codes %>

  </div>
</div>