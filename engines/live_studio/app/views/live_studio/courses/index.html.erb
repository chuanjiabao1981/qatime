<div class="qa_box">
  <div class="qa_big_cell">
    <div>
      <h2> <%= LiveStudio::Course.model_name.human %></h2>
    </div>

    <div>
      <%= form_tag("", method: "get") do -%>
        <%= label_tag(:category, 'category') %>
        <%= select_tag :category, options_for_select(APP_CONSTANT["grades_in_menu"]) %>

        <%= label_tag(:subjects, 'subjects') %>
        <%= select_tag :subjects, options_for_select(APP_CONSTANT["subjects"]) %>

        <%= label_tag(:teacher, 'teacher') %>
        <%= text_field_tag :teacher %>
        <%= submit_tag "搜索" %>
      <% end -%>
    </div>

  </div>
  <div>
    <%= render @courses %>
  </div>
</div>