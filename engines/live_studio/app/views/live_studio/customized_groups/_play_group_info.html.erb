<div class="course-item">
  <div class="nav-item">
    <span><%= t('view.live_studio/courses.show.introduce') %></span>
  </div>
  <h2 class="remedial-classname"><%= @customized_group.name %></h2>
  <div class="course-lessons">
    <div class="lessons-detail">
      <h3><%= t('view.live_studio/courses.show.base_info') %></h3>
      <ul>
        <li>
          <span>
            <i><%= LiveStudio::Course.human_attribute_name(:subject) %></i>
            <i><%= @customized_group.subject %></i>
          </span>
          <span>
            <i><%= LiveStudio::Course.human_attribute_name(:grade) %></i>
            <i><%= @customized_group.grade %></i>
          </span>
          <span>
            <i><%= t("view.course_show.lessons_count") %></i>
            <i><%= t("view.course_show.lessons_count_of", lessons_count: @customized_group.events_count) %></i>
          </span>
        </li>
        <li>
          <span>
            <i><%= t("activerecord.view.date") %></i>
            <i><%= t("view.course_show.date_range", start_at: @customized_group.start_at.to_date.to_s, end_at: @customized_group.end_at.to_date.to_s) if @customized_group.start_at && @customized_group.end_at %></i>
          </span>
        </li>
        <li>
          <span>
            <i><%= LiveStudio::Course.human_attribute_name(:status) %></i>
            <i><%= @customized_group.status.text %><%= t('view.course_show.lesson_progress_value', closed_count: @customized_group.closed_events_count, lessons_count: @customized_group.events_count) %></i>
          </span>
        </li>
      </ul>
    </div>

    <div class="lessons-detail">
      <h3><%= LiveStudio::Course.human_attribute_name(:objective) %></h3>
      <p><%= @customized_group.objective %></p>
    </div>
    <div class="lessons-detail">
      <h3><%= LiveStudio::Course.human_attribute_name(:suit_crowd) %></h3>
      <p><%= @customized_group.suit_crowd %></p>
    </div>
    <div class="lessons-detail">
      <h3><%= LiveStudio::Course.human_attribute_name(:course_description) %></h3>
      <p><%= raw(@customized_group.description) %></p>
    </div>
  </div>
</div>
