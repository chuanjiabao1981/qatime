<div class="navbar navbar-inverse <%= "navbar-fixed-top" if defined?(fixed) and fixed %>" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">答疑时间</a>
    </div>

    <div class="navbar-collapse collapse" style="height: 1px;">
      <ul class="nav navbar-nav">
        <li class="<%= "active" if controller_name == 'groups'%>"><%= link_to '课程' , groups_path %></li>
        <li class="<%= "active" if controller_name == 'home' %>"><%=link_to '设置',root_path %></li>
        <!--li class="<%= "active" if controller_name == 'topics' and action_name == 'index' %>"><%= link_to '社区',topics_path %> </li-->
      </ul>
      <ul class="nav navbar-nav navbar-right">

        <% if signed_in? %>
            <li>
                <%= link_to current_user.name,user_home_path,class: "pull-left" %>
            </li>
        <% end %>
        <% if signed_in? %>
            <li>
              <%= link_to '登出', signout_path, :method=>'delete' %>
            </li>
        <% else %>
            <li>
              <%= link_to '登录', signin_path %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
